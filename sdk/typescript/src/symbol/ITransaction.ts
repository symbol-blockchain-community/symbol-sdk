import ISerializable from '../models/ISerializable';
import { Signature, PublicKey, TransactionType } from './models';

export default class ITransaction extends ISerializable {
/*   signature: Signature = new Signature();
  signerPublicKey: PublicKey = new PublicKey();
  type: TransactionType = TransactionType.TRANSFER;
  version: number = 0; */
}